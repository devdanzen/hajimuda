export enum Color {
  ThemeGold = "#B8A864",        // Primary gold color (brand[400])
  ThemeGoldLight = "#D4C991",   // Light gold (brand[200])
  ThemeGoldDark = "#9B8F54",    // Dark gold (brand[600])
  ThemeBlack = "#0D0D0D",       // Deep black (gray[900])
  ThemeGray = "#333333",        // Dark gray (gray[700])
  ThemeWhite = "#FAFAFA",       // Pure white (gray[50])
  ThemeBackground = "#FCFCFC",  // Background white
  Gray = "#6B7280"
}